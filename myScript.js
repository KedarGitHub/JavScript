/*  let
    var
    const

    ; or no ; doesn't matter
 */

    let myName= "Kedar"; //can change value
    var iLike = "Pizza" //Can change value
    const PI = 3.1416  //this is like final in Java, we cannot change it

    let String  = 'This is a string'
    let number= 248
    let bool=false
    let array = [1,2,3,4,5,6,7,"a","Hello"] //array[3]=4
    let undefined_value = undefined
    let null_value = null
    let Nan_value = Nan

    //to store key value pairs
    let objects ={
        "Keyname" : "value", //No need to follow the set of rules for variabe definitions objects["Keyname"]
        keyname1 : "value"  //here we have to follow the set of rules for variabe definitions objects.keyname1
    }

    var b=0;
    if(b){
console.log(b)
    }else{

        console.log("Not defined")
    }